# Base URL of the FastAPI backend (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000

# Bearer token for the /admin review-queue page. Must match the backend's
# ADMIN_TOKEN. Do not commit a real value -- this is a dev convenience only;
# in production the admin should paste the token into the login prompt
# instead of baking it into the build.
